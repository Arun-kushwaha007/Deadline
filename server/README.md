# CollabNest Server

This is the backend for the CollabNest application.

## Benchmarks

To run the performance benchmarks, use the following commands:

- `npm run bench:artillery`
- `npm run bench:autocannon`
- `npm run bench:db`
